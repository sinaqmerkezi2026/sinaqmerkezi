
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Auth tamamilə ləğv edildiyi üçün bütün kolleksiyalara hər kəs üçün tam icazə verilir
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
